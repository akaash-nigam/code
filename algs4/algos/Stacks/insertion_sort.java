Insertion Sort. 
for (int i=2;i<arr.lenght;i++)
{
    j = i-1;
    while (i>0) && a[j]>a[i])
    {
        swap a[i] with aj[
        int temp;
        temp = a[i];
        a[j]= a[i]
        i = i -1;
    }
}
