public class IntfixtoPostfix(String exp)
{
    int i,k;
    Stack stack<In> = new Stack(exp.length);
    Stack output = new Stack(. 

    for (ch c : String s)
    {
        output.append(s);
    }
    else if (s==‘c’
        stack.push(s)
    else if (s==‘)’_
    else if (s==‘)’;
    while (!isEmpty(stack) && stack.peek()
        output.append(stack.pop);
    if (!isEmpty(stack) && stack.peek
        return(-1)
    else 
    stack.pop. 
    while (!isEmptyStack) && 
    output.append = stack.pop();
    stack.push(s);
    
    while (!isempty(Stack)
        result.append= stack.pop();
    return (output);
}
