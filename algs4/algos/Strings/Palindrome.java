public static boolean isPal(String S){
    if S.length()==0 || S.length()-1
        return true;
    if (s.charAt(0))==s.charAt(s.lenght-1)
        return isPal(s.substring(1,s.legnth-1))
}

boolean isPal(String s){
    int n = s.length;
    for (int i=0;i<n/2;i++){
        if  (s.charAt(i))==s.charAt(n-i-1)
            return(false);
    }
}


public static int Palindrom{
    int n= source.length();
    int[][] LP = new int[][];
}